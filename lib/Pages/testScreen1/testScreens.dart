import 'dart:html' hide VoidCallback;

import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class TestScreen1 extends StatelessWidget {
  // use signup form as template to check onTap function working or not [WORKS]
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey[200],
      body: Center(
        child: SizedBox(
          width: 400,
          child: Card(
            child: Text(
              'Image Screen 1',
              style: TextStyle(
                fontSize: 24.0,
                fontWeight: FontWeight.bold,
                backgroundColor: Colors.black45,
                color: Colors.white,
              ),
            ),
          ),
        ),
      ),
    );
  }
}

class TestScreen2 extends StatelessWidget {
  // use signup form as template to check onTap function working or not [WORKS]
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey[200],
      body: Center(
        child: SizedBox(
          width: 400,
          child: Card(
            child: Text(
              'Image Screen 2',
              style: TextStyle(
                fontSize: 24.0,
                fontWeight: FontWeight.bold,
                backgroundColor: Colors.black45,
                color: Colors.white,
              ),
            ),
          ),
        ),
      ),
    );
  }
}

class TestScreen3 extends StatelessWidget {
  // use signup form as template to check onTap function working or not [WORKS]
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey[200],
      body: Center(
        child: SizedBox(
          width: 400,
          child: Card(
            child: Text(
              'Image Screen 3',
              style: TextStyle(
                fontSize: 24.0,
                fontWeight: FontWeight.bold,
                backgroundColor: Colors.black45,
                color: Colors.white,
              ),
            ),
          ),
        ),
      ),
    );
  }
}

class TestScreen4 extends StatelessWidget {
  // use signup form as template to check onTap function working or not [WORKS]
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey[200],
      body: Center(
        child: SizedBox(
          width: 400,
          child: Card(
            child: Text(
              'Image Screen 4',
              style: TextStyle(
                fontSize: 24.0,
                fontWeight: FontWeight.bold,
                backgroundColor: Colors.black45,
                color: Colors.white,
              ),
            ),
          ),
        ),
      ),
    );
  }
}

class TestScreen5 extends StatelessWidget {
  // use signup form as template to check onTap function working or not [WORKS]
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey[200],
      body: Center(
        child: SizedBox(
          width: 400,
          child: Card(
            child: Text(
              'Image Screen 5',
              style: TextStyle(
                fontSize: 24.0,
                fontWeight: FontWeight.bold,
                backgroundColor: Colors.black45,
                color: Colors.white,
              ),
            ),
          ),
        ),
      ),
    );
  }
}

class TestScreen6 extends StatelessWidget {
  // use signup form as template to check onTap function working or not [WORKS]
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey[200],
      body: Center(
        child: SizedBox(
          width: 400,
          child: Card(
            child: Text(
              'Image Screen 6',
              style: TextStyle(
                fontSize: 24.0,
                fontWeight: FontWeight.bold,
                backgroundColor: Colors.black45,
                color: Colors.white,
              ),
            ),
          ),
        ),
      ),
    );
  }
}
